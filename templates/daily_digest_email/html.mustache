<html>
<head></head>
<body>
<style>
    h1, h2, h3, p, a, li, body, small, span {
        font-family: "Open Sans", Helvetica, Arial, sans-serif;
        color: #333;
    }
    h3, p, h1, h2, ol {
        margin: 0;
    }
    h1#title > a {
        font-size: 18px;
        text-decoration: none;
        color: #00002D;
    }
    #sub_title {
        margin-top: 0px;
        font-size: 14px;
        color: #00002D;
    }
    h2 {
        font-size: 16px;
    }
    .publication > span {
        margin-right: 10px;
    }
    p, .text {
        font-size: 13px;
    }
    .text, .text > p {
        margin: 10px 0;
    }
    .author small, .keywords small {
        font-weight: bold;
    }
    ol.contents li, ol.contents li a {
        font-size: 12px;
        text-decoration: none;
    }
    ol.contents li a {
        white-space: nowrap;
        overflow: hidden;
    }
    .item-header {
        padding: 20px;
        margin-top: 10px;
        background: #ccddff;
        -moz-border-radius: 3px 3px 0 0;
        border-radius: 3px 3px 0 0;
    }
    .item-body {
        padding: 10px 20px 20px 20px;
        border: 1px solid #ccddff;
        -moz-border-radius: 0 0 3px 3px;
        border-radius: 0 0 3px 3px;
    }
    .item-header > h3 > a {
        font-size: 14px;
        text-decoration: none;
        color: #00002D;
    }
    .item-header > .publication > span,
    .item-header > .publication > a
    {
        font-size: 11px;
        color: #00002D;
    }
</style>


<div class="item-header">
    <h1 id="title"><a href="{{url}}">{{title}}</a></h1>
    <h2 id="sub_title">Table of Contents for {{digest_date}}</h2>
</div>


<div class="item-body">
<ol class="contents">
{{#items}}
    <li><a href="#{{slug}}">{{short_title}}</li>
{{/items}}
</ol>
</div>

{{#items}}

<div>

    <div class="item-header">
    <h3><a href="{{url}}" name="{{slug}}">{{title}}</a></h3>
    <p class="publication">
        {{#publication}}<span>{{publication}}</span>{{/publication}}
        <a class="item-link" href="{{url}}">link</a>
    </p>
    </div>

    <div class="item-body">
    {{#author}}
        <p class="author"><small>Author:</small> {{author}}</p>
    {{/author}}

    {{#text}}
    <div class="text">{{{text}}}</div>
    {{/text}}
    
    {{#keywords}}
        <p class="keywords"><small>Keywords:</small> {{keywords}}</p>
    {{/keywords}}
    </div>
</div>

{{/items}}

</body>
</html>
